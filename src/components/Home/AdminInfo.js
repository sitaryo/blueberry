import React, {Component} from 'react';
import PropTypes from 'prop-types';

class AdminInfo extends Component {
    render() {
        return (
            <div>
                <h2 style={{color:'white'}}>this is admin info</h2>
            </div>
        );
    }
}

AdminInfo.propTypes = {};

export default AdminInfo;
