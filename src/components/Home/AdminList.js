import React, {Component} from 'react';
import PropTypes from 'prop-types';

class AdminList extends Component {
    render() {
        return (
            <div>
                <h2 style={{color:'white'}}>this is admin list</h2>
            </div>
        );
    }
}

AdminList.propTypes = {};

export default AdminList;
